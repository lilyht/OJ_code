#include <iostream>
#include <string.h>
using namespace std;
const int MAXN = 105;

char a[MAXN], b[MAXN];
int main() {
	while(~scanf("%s %s", a, b) && (strcmp(a,"0")||strcmp(b,"0"))) {
		int lena = strlen(a);
		int lenb = strlen(b);
		//没做出来 
	} 
	return 0;
} 
